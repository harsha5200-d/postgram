{
  "name": "baceked-root",
  "version": "1.0.0",
  "scripts": {
    "backend": "npm start --prefix BACKEND",
    "frontend": "npm run dev --prefix frontend/user_interface",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
